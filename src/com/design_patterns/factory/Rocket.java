package com.design_patterns.factory;

public class Rocket extends Ship {
        public Rocket(String name, double damageRate)
        {
            setName(name);
            setDamage(damageRate);
        }
}
